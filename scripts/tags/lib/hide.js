/**
 * hide.js v1.1 | https://github.com/weekdaycare/hexo-theme-stellar/
 * 格式与官方标签插件一致，使用空格分隔；中括号内的是可选参数（中括号不需要写出来）
 *
 * {% hide %}{% endhide %}
 */

'use strict'

module.exports = (ctx) => {
  return function (args) {
    return "";
  }
}
